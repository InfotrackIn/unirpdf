version: 1.0
runtime: python3

build:
  commands:
    build:
      - pip install -r requirements.txt

run:
  # Este es el comando de producción estándar.
  # Gunicorn gestiona los procesos y usa los "workers" de Uvicorn para la velocidad.
  # Gunicorn usará automáticamente la variable $PORT.
  command: gunicorn -w 4 -k uvicorn.workers.UvicornWorker main:app
  network:
    port: 8080